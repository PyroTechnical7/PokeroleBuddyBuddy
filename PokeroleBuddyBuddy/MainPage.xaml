<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PokeroleBuddyBuddy.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Button
                x:Name="ImportItemsBtn"
                Text="Import ItemsCollection.json" 
                SemanticProperties.Hint="Import the items Json"
                Clicked="OnImportItemsClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="ImportAbilitiesBtn"
                Text="Import AbilityDataCollection.json" 
                SemanticProperties.Hint="Import the Ability Collection Json"
                Clicked="OnImportAbilitiesClicked"
                HorizontalOptions="Fill" />

            <Button
                x:Name="ImportMovesBtn"
                Text="Import MoveDataCollection.json" 
                SemanticProperties.Hint="Import the Move Collection Json"
                Clicked="OnImportMovesClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
